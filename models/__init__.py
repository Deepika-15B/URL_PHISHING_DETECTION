"""
models/__init__.py
==================
Public interface for the phishing-detection models package.

Sub-modules (implemented in Phase 3):
    classifier        — Ensemble classifier (Random Forest, XGBoost, LightGBM)
    feature_selector  — Feature importance ranking & selection utilities
    evaluator         — Cross-validation, metrics, confusion-matrix generation
    explainer         — SHAP / LIME model interpretability wrappers
"""
