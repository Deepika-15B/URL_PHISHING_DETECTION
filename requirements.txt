# ============================================================
# Phishing Detection IEEE — Requirements
# ============================================================
# Python >= 3.10 recommended

# ── Web Framework ────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
Werkzeug==3.0.3

# ── Data Handling ────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5           # Excel I/O for datasets

# ── Feature Extraction ───────────────────────────────────────
tldextract==5.1.2          # TLD / subdomain parsing
requests==2.32.3           # HTTP requests for URL inspection
beautifulsoup4==4.12.3     # HTML parsing
lxml==5.2.2                # Fast HTML/XML parser (bs4 backend)
dnspython==2.6.1           # DNS record lookups
whois==0.9.27              # WHOIS domain age queries
python-whois==0.9.4        # Alternative WHOIS library
urllib3==2.2.2             # URL utilities

# ── Machine Learning (reserved for future phases) ────────────
scikit-learn==1.5.1
xgboost==2.1.1
lightgbm==4.4.0
imbalanced-learn==0.12.3   # SMOTE / oversampling

# ── Deep Learning (reserved for future phases) ───────────────
torch==2.3.1
transformers==4.42.4       # BERT-based URL/text models

# ── Model Persistence ────────────────────────────────────────
joblib==1.4.2
pickle5==0.0.12; python_version < "3.8"

# ── Explainability ───────────────────────────────────────────
shap==0.45.1
lime==0.2.0.1

# ── Visualisation ────────────────────────────────────────────
matplotlib==3.9.1
seaborn==0.13.2
plotly==5.22.0

# ── Jupyter / Notebooks ──────────────────────────────────────
jupyter==1.0.0
notebook==7.2.1
ipykernel==6.29.5

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1       # Environment variable management
loguru==0.7.2              # Structured logging
tqdm==4.66.4               # Progress bars
pyyaml==6.0.2              # YAML config files
click==8.1.7               # CLI utilities

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.0              # Async-capable test client for Flask

# ── Code Quality ─────────────────────────────────────────────
black==24.4.2
flake8==7.1.0
isort==5.13.2
mypy==1.10.1
