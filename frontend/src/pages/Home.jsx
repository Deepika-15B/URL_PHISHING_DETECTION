import {useState} from 'react'; import axios from 'axios'; import {Copy,Search,ShieldAlert,ShieldCheck} from 'lucide-react'; import {motion} from 'framer-motion'; import {Gauge,Section} from '../components/UI';
export default function Home({add}){const [url,setUrl]=useState(''),[loading,setLoading]=useState(false),[result,setResult]=useState(null),[error,setError]=useState('');const go=async e=>{e.preventDefault();setLoading(true);setError('');try{const {data}=await axios.post('/predict',{url});setResult(data);add(data)}catch(x){setError(x.response?.data?.error||'Unable to reach the detection API.')}finally{setLoading(false)}};const copy=()=>navigator.clipboard.writeText(JSON.stringify(result,null,2));const bad=result?.prediction==='Phishing';return <><section className="py-12 text-center"><motion.p initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} className="font-semibold text-cyan-600">INTELLIGENT URL DEFENSE</motion.p><h1 className="mx-auto mt-3 max-w-4xl text-4xl font-black tracking-tight md:text-6xl">AI Powered IEEE Phishing URL Detection</h1><p className="mx-auto mt-5 max-w-2xl text-lg text-slate-500">Detect phishing websites in real time using Machine Learning and URL Feature Analysis.</p><form onSubmit={go} className="glass mx-auto mt-8 flex max-w-3xl gap-3 rounded-2xl p-3"><input aria-label="Website URL" className="input" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com" required/><button className="btn" disabled={loading}>{loading?<><Search className="animate-spin"/>Scanning URL...</>: 'Detect'}</button></form>{loading&&<p className="mt-4 animate-pulse text-cyan-600">Extracting IEEE Features · Running Neural Network · Generating Prediction</p>}{error&&<p role="alert" className="mt-4 text-danger">{error}</p>}</section>{result&&<motion.section initial={{opacity:0,scale:.96}} animate={{opacity:1,scale:1}} className={`mx-auto flex max-w-2xl flex-col items-center gap-6 rounded-2xl p-7 text-center ${bad?'bg-rose-500/10 border border-rose-500/30':'bg-emerald-500/10 border border-emerald-500/30'} md:flex-row`}><Gauge value={result.confidence} danger={bad}/><div className="flex-1"><div className="flex justify-center gap-2 text-2xl font-bold">{bad?<ShieldAlert className="text-danger"/>:<ShieldCheck className="text-safe"/>}{result.prediction}</div><p className="mt-2">Probability: {(result.probability*100).toFixed(2)}% · Confidence: {result.confidence}%</p><button onClick={copy} className="mt-4 inline-flex gap-2 text-sm text-cyan-600"><Copy size={16}/>Copy result</button></div></motion.section>}</>}

