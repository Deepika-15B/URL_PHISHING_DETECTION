import {useMemo,useState} from 'react'; import {Trash2} from 'lucide-react'; import {Section} from '../components/UI'; export default function History({history}){const[q,setQ]=useState(''),[filter,setFilter]=useState('All');const rows=useMemo(()=>history.items.filter(x=>(filter==='All'||x.prediction===filter)&&x.url.toLowerCase().includes(q.toLowerCase())),[history.items,q,filter]);return <Section title="Detection History" sub="Stored locally in your browser."><div className="mb-4 flex flex-wrap gap-3"><input className="input max-w-sm" placeholder="Search URLs" value={q} onChange={e=>setQ(e.target.value)}/>{['All','Safe','Phishing'].map(x=><button key={x} onClick={()=>setFilter(x)} className="btn !px-3 !py-2">{x}</button>)}<button onClick={history.clear} className="ml-auto text-danger">Clear history</button></div><div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th>Date / Time</th><th>URL</th><th>Prediction</th><th>Probability</th><th>Confidence</th><th/></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-b border-slate-200/40"><td>{new Date(x.date).toLocaleString()}</td><td className="max-w-xs truncate">{x.url}</td><td>{x.prediction}</td><td>{(x.probability*100).toFixed(2)}%</td><td>{x.confidence}%</td><td><button aria-label="Delete history item" onClick={()=>history.remove(x.id)}><Trash2 size={16}/></button></td></tr>)}</tbody></table></div></Section>}

